# Çift Dil (TR/EN) — Aşama 0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** i18n altyapısını kurmak; sağ üstte sabit bir dil değiştirici ve iki dilli (TR/EN) sol menü eklemek.

**Architecture:** `react-i18next` ile global bir i18n örneği; çeviriler `en.json`/`tr.json` içinde alana göre gruplu; dil tercihi `localStorage('appLang')`'da; varsayılan İngilizce. Menü öğeleri `title` yerine `titleKey` tutar, `PageLayout` çizerken `t()` ile çevirir. Sabit dil değiştirici `App.tsx`'te render edilir.

**Tech Stack:** TypeScript, React, react-i18next, i18next, Vite.

## Global Constraints

- Varsayılan dil **İngilizce**; `localStorage('appLang')` boşsa `'en'` — tarayıcı diline bakılmaz, `languagedetector` kullanılmaz (verbatim spec).
- Diller yalnızca `en`, `tr`. İkiden fazla dil yok, tarih/sayı/para yerelleştirmesi yok (yalnızca metin).
- `en.json` ve `tr.json` **birebir aynı anahtar kümesine** sahip olmalı.
- Çeviri anahtarları alana göre gruplu: `nav.*`, `common.*`.
- Formal test yok; doğrulama `npx tsc` + `npm run build` + manuel/görsel.

---

### Task 1: i18n altyapısı (paket + config + çeviri kaynakları)

**Files:**
- Create: `client/src/lib/i18n.ts`
- Create: `client/src/locales/en.json`
- Create: `client/src/locales/tr.json`
- Modify: `package.json` (bağımlılıklar — `npm install` ile)

**Interfaces:**
- Produces: default export `i18n` (i18next örneği) `client/src/lib/i18n.ts`'ten; `en.json`/`tr.json` `nav.*` ve `common.*` anahtarları.

- [ ] **Step 1: Bağımlılıkları kur**

Run: `npm install i18next@^23 react-i18next@^14`
Expected: package.json'a `i18next` ve `react-i18next` eklenir, hata yok.

- [ ] **Step 2: İngilizce çeviri dosyasını oluştur**

`client/src/locales/en.json`:
```json
{
  "nav": {
    "dashboard": "Dashboard",
    "procedures": "Procedures",
    "expenses": "Expenses",
    "payments": "Payments",
    "taxCalculation": "Tax Calculation",
    "storageCalculator": "Storage Calculator",
    "reports": "Reports",
    "bulkDownload": "Bulk Download",
    "askCnc": "Ask CNC?",
    "invoiceMaker": "Invoice Maker",
    "settings": "Settings"
  },
  "common": {
    "logout": "Logout",
    "loggingOut": "Logging out...",
    "loading": "Loading..."
  }
}
```

- [ ] **Step 3: Türkçe çeviri dosyasını oluştur**

`client/src/locales/tr.json`:
```json
{
  "nav": {
    "dashboard": "Panel",
    "procedures": "İşlemler",
    "expenses": "Giderler",
    "payments": "Ödemeler",
    "taxCalculation": "Vergi Hesaplama",
    "storageCalculator": "Ardiye Hesaplama",
    "reports": "Raporlar",
    "bulkDownload": "Toplu İndirme",
    "askCnc": "CNC'ye Sor",
    "invoiceMaker": "Fatura Oluşturucu",
    "settings": "Ayarlar"
  },
  "common": {
    "logout": "Çıkış Yap",
    "loggingOut": "Çıkış yapılıyor...",
    "loading": "Yükleniyor..."
  }
}
```

- [ ] **Step 4: i18n config dosyasını oluştur**

`client/src/lib/i18n.ts`:
```ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import en from "../locales/en.json";
import tr from "../locales/tr.json";

const saved = localStorage.getItem("appLang") || "en";

i18n.use(initReactI18next).init({
  resources: {
    en: { translation: en },
    tr: { translation: tr },
  },
  lng: saved,
  fallbackLng: "en",
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **Step 5: JSON paritesini doğrula (en/tr aynı anahtarlar)**

Run:
```bash
python -c "
import json
e=json.load(open('client/src/locales/en.json'))
t=json.load(open('client/src/locales/tr.json'))
def keys(d,p=''):
    s=set()
    for k,v in d.items():
        s.add(p+k)
        if isinstance(v,dict): s|=keys(v,p+k+'.')
    return s
ek,tk=keys(e),keys(t)
print('SADECE en:', ek-tk)
print('SADECE tr:', tk-ek)
print('OK' if ek==tk else 'PARITE HATASI')
"
```
Expected: `SADECE en: set()` / `SADECE tr: set()` / `OK`

- [ ] **Step 6: Tip kontrolü**

Run: `npx tsc 2>&1 | grep -iE "i18n|locales" || echo "OK"`
Expected: `OK`

> Not: JSON import için `tsconfig`'de `resolveJsonModule` gerekir. Vite/TS projelerinde genelde açıktır; `OK` çıkmazsa ve hata `Cannot find module '../locales/en.json'` ise `tsconfig.json`'daki `compilerOptions`'a `"resolveJsonModule": true` ekle ve adımı tekrarla.

- [ ] **Step 7: Commit**

```bash
git add client/src/lib/i18n.ts client/src/locales/en.json client/src/locales/tr.json package.json package-lock.json
git commit -m "feat(i18n): add react-i18next infra + nav/common translations"
```

---

### Task 2: Sabit dil değiştirici + uygulamaya bağlama

**Files:**
- Create: `client/src/components/LanguageSwitcher.tsx`
- Modify: `client/src/main.tsx` (i18n import)
- Modify: `client/src/App.tsx` (LanguageSwitcher render)

**Interfaces:**
- Consumes: `i18n` (Task 1, global; `useTranslation` ile erişilir).
- Produces: `LanguageSwitcher` adlı named export bileşen.

- [ ] **Step 1: LanguageSwitcher bileşenini oluştur**

`client/src/components/LanguageSwitcher.tsx`:
```tsx
import { useTranslation } from "react-i18next";

export function LanguageSwitcher() {
  const { i18n } = useTranslation();

  const setLang = (lng: "tr" | "en") => {
    i18n.changeLanguage(lng);
    localStorage.setItem("appLang", lng);
  };

  const current = i18n.language?.startsWith("tr") ? "tr" : "en";

  const cls = (active: boolean) =>
    `px-2 py-0.5 text-sm rounded ${
      active ? "bg-primary text-primary-foreground font-medium" : "text-muted-foreground hover:text-foreground"
    }`;

  return (
    <div className="fixed top-2 right-2 z-50 flex items-center gap-1 rounded-md border bg-background/80 backdrop-blur-sm px-1 py-0.5 shadow-sm">
      <button type="button" onClick={() => setLang("tr")} className={cls(current === "tr")}>TR</button>
      <span className="text-muted-foreground text-xs">|</span>
      <button type="button" onClick={() => setLang("en")} className={cls(current === "en")}>EN</button>
    </div>
  );
}
```

- [ ] **Step 2: i18n'i uygulama girişinde yükle**

`client/src/main.tsx`'i şu hale getir (i18n import'u App'ten önce):
```tsx
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";
import "./lib/i18n";

// Add title to the document
document.title = "Blank Project with Sidebar";

createRoot(document.getElementById("root")!).render(<App />);
```

- [ ] **Step 3: LanguageSwitcher'ı App'e ekle**

`client/src/App.tsx` içinde:
- Importlara ekle: `import { LanguageSwitcher } from "@/components/LanguageSwitcher";`
- `App()` fonksiyonundaki `return` bloğunu şununla değiştir:
```tsx
  return (
    <QueryClientProvider client={queryClient}>
      <LanguageSwitcher />
      <Router />
      <Toaster />
    </QueryClientProvider>
  );
```

- [ ] **Step 4: Tip kontrolü**

Run: `npx tsc 2>&1 | grep -iE "LanguageSwitcher|App.tsx|main.tsx" || echo "OK"`
Expected: `OK`

- [ ] **Step 5: Build doğrulaması**

Run: `npm run build 2>&1 | tail -5`
Expected: Vite build başarılı (hata yok, `dist/` üretilir).

- [ ] **Step 6: Commit**

```bash
git add client/src/components/LanguageSwitcher.tsx client/src/main.tsx client/src/App.tsx
git commit -m "feat(i18n): fixed top-right language switcher, wire i18n into app"
```

---

### Task 3: Menüyü iki dilli yap (nav-items + PageLayout)

**Files:**
- Modify: `client/src/lib/nav-items.ts`
- Modify: `client/src/components/layout/PageLayout.tsx`

**Interfaces:**
- Consumes: `nav.*` ve `common.*` anahtarları (Task 1); `useTranslation` (react-i18next).
- Produces: `NavItem` tipi artık `titleKey: string` taşır (önceki `title` değil).

- [ ] **Step 1: nav-items'i titleKey'e geçir**

`client/src/lib/nav-items.ts` içinde `NavItem` tipini ve diziyi değiştir:
```ts
export type NavItem = {
  titleKey: string;
  url: string;
  icon: ComponentType<any>;
};

export const defaultNavItems: NavItem[] = [
  { titleKey: "nav.dashboard", url: "/dashboard", icon: Home },
  { titleKey: "nav.procedures", url: "/procedures", icon: Inbox },
  { titleKey: "nav.expenses", url: "/expenses", icon: Calendar },
  { titleKey: "nav.payments", url: "/payments", icon: Search },
  { titleKey: "nav.taxCalculation", url: "/tax-calculation", icon: Calculator },
  { titleKey: "nav.storageCalculator", url: "/storage-calculator", icon: Warehouse },
  { titleKey: "nav.reports", url: "/reports", icon: BarChart2 },
  { titleKey: "nav.bulkDownload", url: "/bulk-download", icon: Archive },
  { titleKey: "nav.askCnc", url: "/ask", icon: Sparkles },
  { titleKey: "nav.invoiceMaker", url: "/invoice-maker", icon: FileText },
  { titleKey: "nav.settings", url: "/settings", icon: Settings },
];
```

- [ ] **Step 2: PageLayout'ta useTranslation ekle ve menüyü çevir**

`client/src/components/layout/PageLayout.tsx`:
- En üste import ekle: `import { useTranslation } from "react-i18next";`
- `PageLayout` fonksiyonunun başına (`const navItems = defaultNavItems;` satırının altına) ekle:
```tsx
  const { t } = useTranslation();
```
- Menü `map` bloğunu (mevcut `key={item.title}` / `tooltip={item.title}` / `<span>{item.title}</span>`) şununla değiştir:
```tsx
                    {navItems.map((item) => (
                      <SidebarMenuItem key={item.titleKey}>
                        <SidebarMenuButton
                          asChild
                          tooltip={t(item.titleKey)}
                          className="text-[1.1em]"
                        >
                          <a href={item.url}>
                            <item.icon />
                            <span>{t(item.titleKey)}</span>
                          </a>
                        </SidebarMenuButton>
                      </SidebarMenuItem>
                    ))}
```

- [ ] **Step 3: PageLayout footer metinlerini çevir**

Aynı dosyada footer'daki sabit İngilizce metinleri değiştir:
- `{currentUser?.username || 'Loading...'}` → `{currentUser?.username || t('common.loading')}`
- `{currentUser?.email || 'Loading...'}` → `{currentUser?.email || t('common.loading')}`
- `{logoutMutation.isPending ? 'Logging out...' : 'Logout'}` → `{logoutMutation.isPending ? t('common.loggingOut') : t('common.logout')}`

> Not: Logout sonrası `toast` mesajları ("Logged out" vb.) bu aşamanın kapsamı dışıdır (kullanıcı etkileşimiyle çıkan geçici bildirimler); ileride bir aşamada çevrilir.

- [ ] **Step 4: Tip kontrolü**

Run: `npx tsc 2>&1 | grep -iE "nav-items|PageLayout" || echo "OK"`
Expected: `OK`

- [ ] **Step 5: Eski `title` kullanımının kalmadığını doğrula**

Run: `grep -rn "item.title\b" client/src/components/layout/PageLayout.tsx || echo "OK: eski title kullanımı yok"`
Expected: `OK: eski title kullanımı yok`

- [ ] **Step 6: Build doğrulaması**

Run: `npm run build 2>&1 | tail -5`
Expected: Vite build başarılı.

- [ ] **Step 7: Commit**

```bash
git add client/src/lib/nav-items.ts client/src/components/layout/PageLayout.tsx
git commit -m "feat(i18n): translate sidebar menu + layout footer (TR/EN)"
```

---

### Task 4: Yayın ve doğrulama

**Files:** yok (deploy + doğrulama)

- [ ] **Step 1: Push (otomatik deploy)**

```bash
git push origin main
```

- [ ] **Step 2: Deploy'u izle**

```bash
RUN=$(gh run list --workflow=deploy.yml --limit 1 --json databaseId -q '.[0].databaseId')
gh run watch "$RUN" --exit-status
```
Expected: deploy exit 0, `OK /mcp/health`.

- [ ] **Step 3: Kullanıcı manuel doğrulaması**

Kullanıcıdan teyit iste (tarayıcıda):
- Sağ üstte sabit **TR | EN** düğmesi her sayfada görünüyor ve scroll'da kaybolmuyor.
- Varsayılan İngilizce; **TR**'ye basınca sol menü Türkçeleşiyor (Panel, İşlemler, Giderler...), **EN**'e basınca İngilizce'ye dönüyor.
- Sayfa yenilenince (F5) seçilen dil korunuyor.
- Çevrilmemiş sayfa içerikleri İngilizce kalıyor (beklenen — sonraki aşamalar).

---

## Sonraki aşamalar (şablon — bu plan kapsamı dışı, ayrı planlar)

Her sayfa için tekrarlanan desen:
1. Sayfa dosyasında `import { useTranslation } from "react-i18next";` + `const { t } = useTranslation();`.
2. Sayfanın gömülü İngilizce metinlerini `t('<sayfa>.<anahtar>')` ile değiştir.
3. `en.json` ve `tr.json`'a `<sayfa>` grubunu ekle (parite korunur — Task 1 Step 5 kontrolü).
4. tsc + build + manuel test + commit.

Önerilen sıra: dashboard → procedures → payments → expenses → tax-calculation → storage-calculator → reports → bulk-download → ask → invoice-maker → settings → kalan alt sayfalar.

## Self-Review Notları

- **Spec kapsamı:** Aşama 0'ın tüm bileşenleri (i18n config Task 1, locale dosyaları Task 1, LanguageSwitcher Task 2, main/App bağlama Task 2, nav titleKey + PageLayout çeviri Task 3) karşılandı. Varsayılan İngilizce (Task 1 i18n.ts), sabit sağ üst değiştirici (Task 2), localStorage `appLang` (Task 1 + Task 2). ✓
- **Placeholder:** yok; tüm kod tam.
- **Tip tutarlılığı:** `NavItem.titleKey` Task 3'te tanımlandı, PageLayout aynı adı kullanır. `appLang` anahtarı i18n.ts ve LanguageSwitcher'da aynı. `LanguageSwitcher` named export, App named import. ✓
- **Risk:** `resolveJsonModule` kapalıysa Task 1 Step 6 not'u devreye girer.
